import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Brand
  static const Color primary = Color(0xFF283593);
  static const Color primaryDark = Color(0xFF1A237E);
  static const Color primaryMedium = Color(0xFF3949AB);
  static const Color primaryLight = Color(0xFF5C6BC0);
  static const Color secondary = Color(0xFF03DAC6);

  // Surfaces and backgrounds
  static const Color appBackground = Color(0xFFF4F7FB);
  static const Color surface = Colors.white;
  static const Color surfaceMuted = Color(0xFFF8FAFC);
  static const Color surfaceAccent = Color(0xFFE8EAF6);

  // Text and icons
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color iconInactive = Color(0xFFCBD5E1);

  // Borders and effects
  static const Color border = Color(0xFFE2E8F0);
  static const Color shadow = Color(0x2618273D);
  static const Color transparent = Colors.transparent;

  // Status
  static const Color danger = Colors.red;
  static const Color onDark = Colors.white;
  static const Color black54 = Colors.black54;
}
